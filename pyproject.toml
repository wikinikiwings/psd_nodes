[project]
name = "comfyui-psd-export"
description = "Export ComfyUI masks as a layered PSD: one layer per prompt, each with an editable Photoshop layer mask"
version = "0.1.0"
license = { text = "MIT" }
dependencies = ["psd-tools>=1.18"]

[project.urls]
Repository = "https://github.com/wikinikiwings/comfyui-psd-export"

[tool.comfy]
PublisherId = "wikinikiwings"
DisplayName = "PSD Export"
Icon = ""
